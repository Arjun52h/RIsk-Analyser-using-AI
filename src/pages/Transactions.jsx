import{useMemo,useState}from'react';import TransactionTable from'../components/TransactionTable';
export default function Transactions({rows,onSelect}){const[q,setQ]=useState(""),[f,setF]=useState("ALL");const list=useMemo(()=>rows.filter(t=>(!q||t.id.toLowerCase().includes(q.toLowerCase())||t.customerId.toLowerCase().includes(q.toLowerCase()))&&(f==="ALL"||t.risk.decision===f)),[rows,q,f]);return <div className="content"><div className="pageTitle"><div className="eyebrow">TRANSACTION MONITOR</div><h2>Payment Transactions</h2><p>Search and review transactions scored by the local risk engine.</p></div><div className="toolbar"><input placeholder="Search transaction or customer..." value={q} onChange={e=>setQ(e.target.value)}/><div>{["ALL","ALLOW","REVIEW","BLOCK"].map(x=><button className={f===x?"filter active":"filter"} onClick={()=>setF(x)} key={x}>{x}</button>)}</div></div><div className="result-count">{list.length} transaction(s) shown</div><section className="panel"><TransactionTable rows={list} onSelect={onSelect}/></section></div>}