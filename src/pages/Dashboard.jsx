import StatCard from "../components/StatCard";import TransactionTable from "../components/TransactionTable";
export default function Dashboard({rows,onSelect}){const total=rows.length,safe=rows.filter(x=>x.risk.decision==="ALLOW").length,review=rows.filter(x=>x.risk.decision==="REVIEW").length,block=rows.filter(x=>x.risk.decision==="BLOCK").length,exposure=rows.reduce((s,x)=>s+x.risk.exposure,0),risk=((review+block)/total*100).toFixed(1),suspicious=rows.filter(x=>x.risk.decision!=="ALLOW").sort((a,b)=>b.risk.score-a.risk.score).slice(0,7);const max=Math.max(safe,review,block,1);const counts=rows.flatMap(x=>x.risk.factors.map(f=>f.label)).reduce((a,k)=>(a[k]=(a[k]||0)+1,a),{});return <div className="content"><div className="stats"><StatCard label="Total Transactions" value={total} meta="Local transaction stream"/><StatCard label="Safe Transactions" value={safe} meta={`${(safe/total*100).toFixed(1)}% of volume`}/><StatCard label="Under Review" value={review} meta="Step-up verification"/><StatCard label="High Risk" value={block} meta="Automatic block"/></div><div className="riskHero"><div><div className="eyebrow">POTENTIAL RISK EXPOSURE</div><h2>₹{(exposure/100000).toFixed(1)}L</h2><p>Estimated value associated with review or block decisions.</p></div><div className="ring"><b>{risk}%</b><span>Risk Rate</span></div></div><div className="two"><section className="panel chartPanel"><div className="eyebrow">RISK DISTRIBUTION</div><h2>Decision volume</h2><div className="bars">{[["ALLOW",safe,"allow"],["REVIEW",review,"review"],["BLOCK",block,"block"]].map(([l,v,c])=><div className="barRow" key={l}><span>{l}</span><div className="barBg"><i className={c} style={{width:`${Math.max(4,v/max*100)}%`}}/></div><b>{v}</b></div>)}</div></section><section className="panel chartPanel"><div className="eyebrow">RISK SIGNALS</div><h2>Most frequent triggers</h2><div>{Object.entries(counts).sort((a,b)=>b[1]-a[1]).slice(0,4).map(([k,v])=><div className="signal" key={k}><span>{k}</span><b>{v}</b></div>)}</div></section></div><section className="panel"><div className="panelHead"><div><div className="eyebrow">RECENT ACTIVITY</div><h2>Transactions requiring attention</h2></div></div><TransactionTable rows={suspicious} onSelect={onSelect}/></section><div className="two"><section className="panel smallPanel"><div className="eyebrow">DECISION MIX</div><div className="mix"><div><small>ALLOW</small><b>{safe}</b></div><div><small>REVIEW</small><b>{review}</b></div><div><small>BLOCK</small><b>{block}</b></div></div></section><section className="panel smallPanel"><div className="eyebrow">ENGINE STATUS</div><p className="online">● All local risk rules operational</p><p className="muted">100% local processing · No external API</p></section></div></div>}